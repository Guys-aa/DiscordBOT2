services:
  - type: web
    name: prim-bot2
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot2.py
    envVars:
      - key: DISCORD_TOKEN2
        sync: false
    healthCheckPath: /health
